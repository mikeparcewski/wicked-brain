name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write
  id-token: write
  pull-requests: write

jobs:
  release:
    uses: mikeparcewski/wicked-ci/.github/workflows/node-release.yml@6573ae1b5eeb50468193308750c08ceeafde8039 # v1.1.1
    with:
      package_name: wicked-brain
      test_working_dir: server
      version_dirs: |
        .
        server
      pre_publish_install_dirs: |
        .
        server
      enable_sync_pr: true
    secrets: inherit
